# Optional frontend file: copy to .env.local.
# npm run demo forces mock mode regardless of this setting.
# npm run dev uses api by default. Do not place credentials in any VITE_ variable.
VITE_NEWS_PROVIDER=api
