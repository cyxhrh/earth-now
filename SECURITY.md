# 安全报告与密钥

请不要在公开 Issue 或 PR 中粘贴密钥、Authorization、数据库连接串、完整运行日志或个人数据。优先使用仓库 Security → Advisories → Report a vulnerability 的私密报告入口。若入口不可用，可先开不含利用细节的 Issue 请求私密沟通方式。

本项目维护者尽力处理报告，但不承诺特定响应时间。请提供受影响版本、最小复现条件、预期边界与脱敏证据。

- 密钥只放在服务端 `.env.translation.local` 或进程环境，不能放在 `VITE_` 变量。
- `.gitignore` 是辅助措施，发布前仍应检查暂存区和历史。
- 如果密钥曾出现在聊天、评论或提交中，应到提供商控制台吊销并更换；删除文字或提交不能恢复密钥的保密性。
- 本项目不会自动吊销密钥，也不会把作者的密钥分发给使用者。
- 真实模式会获取外部网页和图片；新闻文本作为不可信数据处理，不执行其中指令。
- 公网部署需自行保护维护状态、持久目录和反向代理；详细说明见 [自部署](docs/self-hosting.md)。
